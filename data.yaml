# Base dataset directory
path: '.'

# Directories for training and validation images (relative to 'path')
train: train/images
val: valid/images

# Number of classes
nc: 4

# Class names (in correct order)
names:
  0: road
  1: solid_line
  2: dotted_line
  3: finish

